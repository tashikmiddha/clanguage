#include<stdio.h>
int main()
{
    printf("the size of integer data type is %d",sizeof(int));//sizeof is a function which is used to know how many bites of memory is used by the data type
    return 0;
}